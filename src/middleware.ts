import { withAuth } from 'next-auth/middleware';

export default withAuth({
  pages: {
    signIn: '/login',
  },
});

export const config = {
  matcher: ['/studio/:path*', '/profile/:path*', '/settings/:path*'],
};
